{
  "name": "root",
  "version": "2.0.1",
  "description": "React component for showing carousel just like a film strip",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --stream",
    "clean": "lerna run clean --parallel --stream",
    "prepublishOnly": "lerna run prepublishOnly --stream",
    "start": "npm run build && concurrently --kill-others --names \"lerna run start --parallel --stream\" \"npm run start:server\"",
    "start:server": "serve -p 5000",
    "test": "lerna run test"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "lerna": "^3.13.1",
    "react": "16.8.6",
    "react-dom": "16.8.6",
    "serve": "^11.1.0"
  }
}
